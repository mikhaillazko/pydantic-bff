[project]
name = "fastbff"
version = "0.3.0"
description = "Simple back-end for front-end using Pydantic. Declarative data composition with typed transformers, dependency injection, and automatic N+1 avoidance."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [
    { name = "Mikhail Lazko" },
]
keywords = ["pydantic", "bff", "fastapi", "dependency-injection", "data-loader", "n+1"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Framework :: FastAPI",
    "Framework :: Pydantic :: 2",
]
dependencies = [
    "pydantic>=2.0,<3",
    "fastapi>=0.100",
    # Async dispatch bridges coroutines from worker threads onto the running
    # loop with anyio (the same primitive Starlette uses for sync endpoints),
    # so it must be backend-agnostic and explicit rather than transitive.
    "anyio>=3.4",
]

[project.optional-dependencies]
sqlalchemy = ["sqlalchemy>=2.0"]

[project.urls]
Homepage = "https://github.com/mikhaillazko/fastbff"
Issues = "https://github.com/mikhaillazko/fastbff/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=7.0",
    "pytest-cov>=5.0",
    "ruff>=0.5",
    "ty>=0.0.1a5",
    "pre-commit>=3.7",
    "sqlalchemy>=2.0",
    "httpx>=0.27",
]

[tool.hatch.build.targets.sdist]
include = [
    "fastbff",
    "README.md",
    "LICENSE",
]
exclude = [
    "**/*_test.py",
]

[tool.hatch.build.targets.wheel]
packages = ["fastbff"]
exclude = [
    "**/*_test.py",
]

[tool.pytest.ini_options]
testpaths = ["fastbff", "."]
python_files = ["*_test.py"]

[tool.coverage.run]
source = ["fastbff"]
branch = true
omit = ["**/*_test.py", "conftest.py"]

[tool.coverage.report]
show_missing = true
# Floor set just below the current level; ratchet up as coverage improves.
fail_under = 85
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
    "\\.\\.\\.",
]

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM", "TID"]
ignore = ["E501"]

[tool.ruff.lint.isort]
force-single-line = true

[tool.ruff.lint.per-file-ignores]
"*_test.py" = ["B018"]

[tool.ruff.format]
quote-style = "single"

[tool.ty.src]
include = ["fastbff"]
exclude = ["**/*_test.py"]

[tool.ty.rules]
# ty is strict by default; these rules conflict with Pydantic/FastAPI runtime
# dynamism (Annotated aliases, model_fields on generic metaclasses, etc.).
unresolved-attribute = "warn"
invalid-return-type = "warn"
invalid-type-form = "warn"
invalid-method-override = "warn"

[tool.ty.terminal]
# Keep intentionally downgraded diagnostics visible without failing CI.
error-on-warning = false
